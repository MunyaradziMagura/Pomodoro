import { VerticalBox, HorizontalBox, LineEdit, Button } from "std-widgets.slint";
export component taskVisualiser inherits Rectangle {
    min-width: 500px;
    HorizontalBox {
        Text {
            text: "hello world this is where teh text will be adeded";
        }
    }
}
